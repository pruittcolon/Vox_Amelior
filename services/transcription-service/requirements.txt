# Transcription Service Dependencies
# The actual locked dependency list lives in requirements.lock to keep Docker caching stable.
# Keep all edits in that file so the install layer can be reused reliably.
-r requirements.lock

# Optional CUDA bindings are built into the base image; leave commented to document intent.
# cuda-python==12.4.0  # Already present in base CUDA image; leave commented to avoid redundant pip install
