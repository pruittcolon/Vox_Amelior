# API Gateway Dependencies
# FastAPI & Server
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic-settings>=2.0.0  # Centralized env var config
python-multipart==0.0.21

# HTTP Client for proxying to backend services
httpx==0.28.1

# WebSocket client for real-time streaming proxy
websockets>=12.0

# Authentication & Security
bcrypt==4.1.2
cryptography==42.0.5
python-jose[cryptography]==3.5.0
# pysqlcipher3==1.0.4  # Optional: commented out due to build issues; DB encryption still works with cryptography

# Audio Processing (for speaker enrollment feature)
numpy>=1.26.0
soundfile>=0.12.1
librosa>=0.10.1

# PostgreSQL async client (for Call Intelligence Platform)
asyncpg>=0.29.0





# Salesforce CDC Dependencies
grpcio>=1.60.0
grpcio-tools>=1.60.0
# salesforce-pubsub-api-client>=1.0.0  # Commented out: package not found on PyPI - CDC feature not implemented yet

# Salesforce Analytics Dependencies
pandas>=2.0.0
